# S2 — Standard DDL text file [DEMO-CRITICAL]

**Goal:** produce `<feed_slug>_standard_table_creation.txt`.

**Frozen prerequisites:** S1 frozen (readers, resolution, banner).

**Read first:** SPEC 3 (and 2.2, 2.5–2.7 which it shares). Inputs are in `INPUTS.md`; the acceptance configuration is SPEC 0.1.

## Case 1 — typed standard layer with a renamed column (Pair A, feed 2 `syn_gadget_events`)

### Inputs

- FRD: Pair A feed 2.
- STTM: Pair A workbook, sheet `MAPPING-SYN_GADGET_EVENTS` (standard datatypes String, Timestamp, Int, Decimal(12,2), Boolean, BigInt; `gadget_nm` renamed to `gadget_name`).
- FAQ: absent.

### Expected output — `syn_gadget_events_standard_table_creation.txt` (sha spans masked)

```text
-- FRD contract: syn_widget_frd sha256 <<sha256>>
-- STTM contract: STTM mapping contract extracted from syn_widget_sttm.xlsx sha256 <<sha256>>
-- Load-pattern FAQ: sha256 defaults (no FAQ file) — 2 answered, 6 unknown
-- Engineering standards: Client Data Engineering Naming + Coding Standards
-- DDL is engineer-run; the agent never creates target tables (create_tables: false).
-- side-tables omitted from deployment DDL — not in FRD Target Table Name (FRD lists: syn_gadget_events, syn_gadget_events_recycle); omitted: syn_gadget_events_errors, syn_gadget_events_processed_files (CodeGen conventions, kept in ddl/)

--syn.syn_gadget_events

CREATE OR REPLACE TABLE syn.syn_gadget_events (
  gadget_key STRING COMMENT 'Gadget key',
  event_ts TIMESTAMP COMMENT 'Event timestamp',
  gadget_owner STRING COMMENT 'Gadget''s owner name',
  gadget_name STRING COMMENT 'Gadget name',
  event_count INT COMMENT 'Events in window',
  amount DECIMAL(12,2) COMMENT 'Event amount in local currency',
  is_active BOOLEAN COMMENT 'Active flag',
  big_ref BIGINT COMMENT 'Large reference number',
  LOB STRING COMMENT 'Lob',
  SRC_FILE_NAME STRING COMMENT 'Src File Name',
  REC_CREATION_TIME TIMESTAMP COMMENT 'Rec Creation Time',
  REC_UPDATED_TIME TIMESTAMP COMMENT 'Rec Updated Time')
USING delta
COMMENT 'Standard target table for feed Syn Gadget Events (source: SynVendor Analytics)'
CLUSTER BY AUTO
TBLPROPERTIES (
  'delta.autoOptimize.autoCompact' = 'true',
  'delta.autoOptimize.optimizeWrite' = 'true',
  'delta.checkpoint.writeStatsAsJson' = 'false',
  'delta.checkpoint.writeStatsAsStruct' = 'true',
  'delta.checkpointPolicy' = 'v2',
  'delta.enableDeletionVectors' = 'true',
  'delta.enableRowTracking' = 'true',
  'delta.feature.appendOnly' = 'supported',
  'delta.feature.deletionVectors' = 'supported',
  'delta.feature.invariants' = 'supported',
  'delta.feature.rowTracking' = 'supported',
  'delta.feature.v2Checkpoint' = 'supported',
  'delta.parquet.compression.codec' = 'zstd');

ALTER TABLE syn.syn_gadget_events SET TAGS ('DOMAIN' = 'Claims', 'SUBDOMAIN' = 'Gadget Events');
```

sha256 of the block content (UTF-8, LF line endings): `97b949c12cc4d59eb138f4ee7b941e8b1bb7202d9c74a87ef1dece40ee55270e`

### Pass criterion

byte-exact after masking: replace every run of 16 or 64 lowercase hexadecimal characters that immediately follows `sha256 ` with `<<sha256>>` in your file, then diff against the block (LF line endings, UTF-8, no BOM).

## Case 2 — AS-IS segmented feed — one standard table per segment, STRING everywhere but audit (Pair B)

### Inputs

- FRD: Pair B (validation rule 4 carries `AS-IS`).
- STTM: Pair B workbook.
- FAQ: `synthetic_segmented_feed.faq.yaml`.

### Expected output — `synthetic_segmented_feed_standard_table_creation.txt` (sha spans masked)

```text
-- FRD contract: syn_segmented_frd sha256 <<sha256>>
-- STTM contract: STTM mapping contract extracted from syn_segmented_sttm.xlsx sha256 <<sha256>>
-- Load-pattern FAQ: sha256 <<sha256>> — 5 answered, 3 unknown
-- Engineering standards: Client Data Engineering Naming + Coding Standards
-- DDL is engineer-run; the agent never creates target tables (create_tables: false).
-- side-tables omitted from deployment DDL — not in FRD Target Table Name (FRD lists: EXT_SYN_HDR, EXT_SYN_DTL, EXT_SYN_TRL, EXT_SYN_DTL_RECYCLE); omitted: EXT_SYN_DTL_ERRORS, EXT_SYN_DTL_PROCESSED_FILES (CodeGen conventions, kept in ddl/)

--SYN_STD.SYN.EXT_SYN_DTL

CREATE OR REPLACE TABLE SYN_STD.SYN.EXT_SYN_DTL (
  SYN_MEMBER_ID STRING COMMENT 'Synthetic member identifier',
  SYN_WIDGET_OWNER STRING COMMENT 'Synthetic owner name',
  SYN_WIDGET_COLOR STRING COMMENT 'Synthetic color',
  LOB STRING COMMENT 'Lob',
  FILE_TYPE STRING COMMENT 'File Type',
  SRC_FILE_NAME STRING COMMENT 'Src File Name',
  REC_CREATION_TIME TIMESTAMP COMMENT 'Rec Creation Time',
  REC_UPDATED_TIME TIMESTAMP COMMENT 'Rec Updated Time')
USING delta
COMMENT 'Standard target table for feed Synthetic Segmented Feed (source: SyntheticVendor)'
CLUSTER BY AUTO
TBLPROPERTIES (
  'delta.autoOptimize.autoCompact' = 'true',
  'delta.autoOptimize.optimizeWrite' = 'true',
  'delta.checkpoint.writeStatsAsJson' = 'false',
  'delta.checkpoint.writeStatsAsStruct' = 'true',
  'delta.checkpointPolicy' = 'v2',
  'delta.enableDeletionVectors' = 'true',
  'delta.enableRowTracking' = 'true',
  'delta.feature.appendOnly' = 'supported',
  'delta.feature.deletionVectors' = 'supported',
  'delta.feature.invariants' = 'supported',
  'delta.feature.rowTracking' = 'supported',
  'delta.feature.v2Checkpoint' = 'supported',
  'delta.parquet.compression.codec' = 'zstd');

ALTER TABLE SYN_STD.SYN.EXT_SYN_DTL SET TAGS ('DOMAIN' = 'Synthetic', 'SUBDOMAIN' = 'Segmented');

--SYN_STD.SYN.EXT_SYN_HDR

CREATE OR REPLACE TABLE SYN_STD.SYN.EXT_SYN_HDR (
  SYN_FMT_VER STRING COMMENT 'Envelope format version',
  SYN_EXTRACT_DT STRING COMMENT 'Date the file was produced',
  SRC_FILE_NAME STRING COMMENT 'Src File Name',
  REC_CREATION_TIME TIMESTAMP COMMENT 'Rec Creation Time',
  REC_UPDATED_TIME TIMESTAMP COMMENT 'Rec Updated Time')
USING delta
COMMENT 'Standard target table for feed Synthetic Segmented Feed (source: SyntheticVendor)'
CLUSTER BY AUTO
TBLPROPERTIES (
  'delta.autoOptimize.autoCompact' = 'true',
  'delta.autoOptimize.optimizeWrite' = 'true',
  'delta.checkpoint.writeStatsAsJson' = 'false',
  'delta.checkpoint.writeStatsAsStruct' = 'true',
  'delta.checkpointPolicy' = 'v2',
  'delta.enableDeletionVectors' = 'true',
  'delta.enableRowTracking' = 'true',
  'delta.feature.appendOnly' = 'supported',
  'delta.feature.deletionVectors' = 'supported',
  'delta.feature.invariants' = 'supported',
  'delta.feature.rowTracking' = 'supported',
  'delta.feature.v2Checkpoint' = 'supported',
  'delta.parquet.compression.codec' = 'zstd');

ALTER TABLE SYN_STD.SYN.EXT_SYN_HDR SET TAGS ('DOMAIN' = 'Synthetic', 'SUBDOMAIN' = 'Segmented');

--SYN_STD.SYN.EXT_SYN_TRL

CREATE OR REPLACE TABLE SYN_STD.SYN.EXT_SYN_TRL (
  SYN_REC_TYPE STRING COMMENT 'Static text identifying the record as the trailer record. Contains the value ******',
  SYN_REC_CNT STRING COMMENT 'Number of Detail records in the file',
  SRC_FILE_NAME STRING COMMENT 'Src File Name',
  REC_CREATION_TIME TIMESTAMP COMMENT 'Rec Creation Time',
  REC_UPDATED_TIME TIMESTAMP COMMENT 'Rec Updated Time')
USING delta
COMMENT 'Standard target table for feed Synthetic Segmented Feed (source: SyntheticVendor)'
CLUSTER BY AUTO
TBLPROPERTIES (
  'delta.autoOptimize.autoCompact' = 'true',
  'delta.autoOptimize.optimizeWrite' = 'true',
  'delta.checkpoint.writeStatsAsJson' = 'false',
  'delta.checkpoint.writeStatsAsStruct' = 'true',
  'delta.checkpointPolicy' = 'v2',
  'delta.enableDeletionVectors' = 'true',
  'delta.enableRowTracking' = 'true',
  'delta.feature.appendOnly' = 'supported',
  'delta.feature.deletionVectors' = 'supported',
  'delta.feature.invariants' = 'supported',
  'delta.feature.rowTracking' = 'supported',
  'delta.feature.v2Checkpoint' = 'supported',
  'delta.parquet.compression.codec' = 'zstd');

ALTER TABLE SYN_STD.SYN.EXT_SYN_TRL SET TAGS ('DOMAIN' = 'Synthetic', 'SUBDOMAIN' = 'Segmented');
```

sha256 of the block content (UTF-8, LF line endings): `d5e19fb54004ac721e38ecb112b8235e2ba24bcc95471fdd45fdbbdd42b4a825`

### Pass criterion

byte-exact after masking: replace every run of 16 or 64 lowercase hexadecimal characters that immediately follows `sha256 ` with `<<sha256>>` in your file, then diff against the block (LF line endings, UTF-8, no BOM).

## Case 3 — stage-only feed — the empty standard file (Pair C `syn_sensor_pings`)

### Inputs

- FRD: Pair C (standard_target.tables empty).
- STTM: Pair C workbook (no Standard band).
- FAQ: `syn_sensor_pings.faq.yaml`.

### Expected output — `syn_sensor_pings_standard_table_creation.txt` (sha spans masked)

```text
-- FRD contract: syn_sensor_frd sha256 <<sha256>>
-- STTM contract: STTM mapping contract extracted from syn_sensor_sttm.xlsx sha256 <<sha256>>
-- Load-pattern FAQ: sha256 <<sha256>> — 2 answered, 6 unknown
-- Engineering standards: Client Data Engineering Naming + Coding Standards
-- DDL is engineer-run; the agent never creates target tables (create_tables: false).
-- side-tables omitted from deployment DDL — not in FRD Target Table Name (FRD lists: syn_sensor_pings); omitted: syn_sensor_pings_errors, syn_sensor_pings_processed_files (CodeGen conventions, kept in ddl/)

-- No standard-layer table for this feed (stage-only load).
```

sha256 of the block content (UTF-8, LF line endings): `70e87b71d58b095c80ea9ed6df7d03d12cf37407bdb6dcf68f1de45aa83e8983`

### Pass criterion

byte-exact after masking: replace every run of 16 or 64 lowercase hexadecimal characters that immediately follows `sha256 ` with `<<sha256>>` in your file, then diff against the block (LF line endings, UTF-8, no BOM).

## If you can't hit this

Re-read the SPEC sections named at the top of this file before touching anything else. Diff your output against the block above line by line; the first differing line names the rule to re-read. If the SPEC is ambiguous about that line, the SPEC is what gets amended (add the missing rule or example there, citing the ACCEPTANCE case that exposed it); never "fix" the expected output, never special-case the acceptance inputs, and never guess: an assumption must quote the FRD field or STTM cell it rests on.
