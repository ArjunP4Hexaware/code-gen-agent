-- GENERATED by codegen-data-engineer-agent — DO NOT EDIT BY HAND.
-- Feed: {{ feed.id }} | processed-files ops table (idempotency + run audit)
-- FRD sha256 {{ provenance.frd_sha[:16] }} | STTM sha256 {{ provenance.sttm_sha[:16] }}
CREATE TABLE IF NOT EXISTS {{ qualified_prefix }}{{ stage_schema }}.{{ processed_files_table }} (
    `SRC_FILE_NAME` STRING,
    `LOB` STRING,
    `ROWS_LOADED` BIGINT,
    `ROWS_REJECTED` BIGINT,
    `ROWS_RECYCLED` BIGINT,
    `REC_CREATION_TIME` TIMESTAMP,
    `REC_UPDATED_TIME` TIMESTAMP
) USING DELTA;
