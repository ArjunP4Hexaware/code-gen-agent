# Generated pipeline — {{ feed.id }}

> GENERATED by codegen-data-engineer-agent — DO NOT EDIT BY HAND.
> Regenerate from the contracts instead; hand edits will be overwritten.

| | |
|---|---|
| Feed | `{{ feed.id }}` ({{ feed.name }}) |
| Source system | {{ feed.source_system }} |
| FRD contract | {{ provenance.frd_name }} — sha256 `{{ provenance.frd_sha[:16] }}` |
| STTM contract | sha256 `{{ provenance.sttm_sha[:16] }}`{% if provenance.synthetic %} — **SYNTHETIC stand-in** — regenerate when the real mapping lands{% endif %} |
| File format | `{{ file_format }}`, delimiter `{{ delimiter }}` |
| Stage schema | `{{ stage_schema }}`{% if default_catalog %} (catalog `{{ default_catalog }}`){% endif %} |
| Load strategy | {{ stage_load_strategy }} (honored as a file-scoped replace via idempotent MERGE on `SRC_FILE_NAME` + `{{ natural_key | join('` + `') }}`) |
{% if recycle %}
| Recycle | `{{ recycle.table }}`, {{ recycle.window_days }}-day window, keyed on `{{ recycle.key_stage_column }}` vs `{{ recycle.reference_id_column }}` in `{{ recycle.reference_table }}`{% if recycle.reference_filter %} where `{{ recycle.reference_filter }}`{% endif %} |
{% endif %}

## Layout

- `{{ feed.slug }}.ipynb` — the whole pipeline assembled into one runnable Databricks notebook (each cell registers itself as `pipeline.<module>`, so the module code runs unmodified); the files below remain the canonical, tested source.
- `ddl/` — Delta / Unity Catalog `CREATE TABLE` statements ({{ segments | length }} stage table{{ "s" if segments | length > 1 }}, errors, processed-files{% if recycle %}, recycle{% endif %}{% if standard %}, standard{% endif %}).
- `pipeline/` — PySpark modules: reader{% if is_segmented %}, segments (Header/Detail/Trailer split){% endif %}, drift, mapping, audit, rejects{% if recycle %}, reference, recycle{% endif %}, writer (MERGE), masking, dq, run_report, orchestrator.
- `job/` — Databricks Workflows job JSON + thin notebook entrypoint.
- `tests/` — pytest suite against a local SparkSession + delta-spark (needs `JAVA_HOME`; Windows also needs `HADOOP_HOME` with winutils).
- `tools/make_fixtures.py` — deterministic sample files from the contract's `sample_value` data.

## Semantics

- Stage lands **all columns as String**, 1:1 names, no casts; typing happens at the standard layer.
- Audit columns (`LOB`, `SRC_FILE_NAME`, `REC_CREATION_TIME`, `REC_UPDATED_TIME`) are pipeline-populated; `run_ts` is injectable so recycle aging and backfills are deterministic.
- Drift: missing column → file FAILs; extra column → quarantined, load proceeds.
- Not-null rejects go to `{{ errors_table }}` with the key masked to last-4{% if phi_columns %} (PHI: `{{ phi_columns | join('`, `') }}`){% endif %}.
- Re-ingesting a file replaces its rows (MERGE); `REC_CREATION_TIME` survives, `REC_UPDATED_TIME` advances.
{% if is_segmented %}
- **ASSUMPTION**: the first field of every row is a record-type discriminator ({% for seg in segments %}`{{ seg.record_type_value }}`={{ seg.name }}{{ ", " if not loop.last }}{% endfor %}). Pending the source dictionary; if the real files differ (e.g. positional header/trailer), regenerate after the contract states it.
{% endif %}

## Running locally

```bash
python tools/make_fixtures.py       # writes data/incoming/*
pytest tests/ -q                    # real Spark; see env note above
```

## DLT portability

Modules map mechanically per `reference/source-to-stage/AGENTS.md` §2:
drift → Auto Loader expectations, dq → DLT expectations, orchestrator →
Workflow/DLT pipeline DAG, masking → Unity Catalog column masks.
